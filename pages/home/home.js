// Get the global app instance
const app = getApp();

Page({
  // Declare page data
  data: {},
  // Listening lifecycle callback onLoad
  onLoad() {
   
  },
  // Listening lifecycle callback onShow
  onShow() {
  },
});
